# Recommended Tools — Reference

Per-stage MCP server recommendations are in `.dev-workflow/references/recommended-tools.md`.

Load that file when you need to suggest tools for a workflow stage.

**When to load**: `/tools` command only.
