# Observability Standards — Reference

Full observability standards (OTel spans, structured logging, metrics, dashboards) are in `.dev-workflow/references/observability.md`.

Load that file when you need detailed observability guidance.

**When to load**: `/observe` and `/staging` stages only.
