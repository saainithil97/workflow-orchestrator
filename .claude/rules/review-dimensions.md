# Review Dimensions — Reference

Full 20-dimension review reference is in `.dev-workflow/references/review-dimensions.md`.

Load that file when running `/review-extended` or when checking specific Tier 2/3 dimensions.

**When to load**: `/review-extended` stage only (or `/review` when Tier 2/3 dimensions are enabled in preferences).
