# <Project Name>

<!-- Generated by /dev-workflow:onboard — customize as needed -->

## About

<Auto-detected project description. Replace or refine.>

- **Language**: <detected language(s)>
- **Framework**: <detected framework(s) or "None">
- **Package manager**: <npm/yarn/pnpm/pip/cargo/go modules/...>

## Architecture

<Auto-detected directory structure with inferred purposes. Example:>

```
src/
├── components/    # UI components
├── services/      # Business logic and external integrations
├── models/        # Data models / types
├── routes/        # API route handlers
├── utils/         # Shared utilities
└── config/        # Configuration and environment
```

## Development

### Build & Run

```bash
<detected build command>
<detected run/start command>
```

### Testing

```bash
<detected test command>
```

- **Test framework**: <detected: jest/vitest/pytest/go test/cargo test/...>
- **Test file pattern**: <detected: *.test.ts / *_test.go / test_*.py / ...>
- **Coverage**: <detected coverage command or "Not configured">

### Linting & Formatting

```bash
<detected lint command or "Not configured">
<detected format command or "Not configured">
```

## Conventions

<Auto-detected patterns. Examples:>

- **Naming**: <camelCase/snake_case/PascalCase for files/functions/classes>
- **Imports**: <absolute/relative, barrel files yes/no>
- **Error handling**: <pattern detected: try/catch, Result types, error returns, ...>
- **Logging**: <library detected or "No structured logging found">

## Key Files

| File | Purpose |
|------|---------|
| <entry point> | Application entry point |
| <config file> | Main configuration |
| <router/routes> | Route definitions |

## Known Constraints

<Auto-detected or manually added constraints:>

- <constraint>

---

## Dev Workflow

This project uses a structured, gated development workflow. Read `.claude/rules/preamble.md` before starting any work.

### Pipeline

```
/requirement → /hld → /lld → /implement → /review → /staging → /docs → /retro
```

For new domains, explore before building: `/explore` → `/spike` → `/crystallize` → `/requirement`

Each stage is gated — do not proceed until the prior stage's document has `status: approved|complete`, `completion.percentage: 100`, and `completion.blockers: []`.

### Tech Debt

Read `.dev-workflow/tech-debt.yml` before editing code. Fix broken windows in files you touch — see `.claude/rules/tech-debt.md`.

### Rules

Load on a need-to-know basis:
- `.claude/rules/workflow.md` — Pipeline stages, gating logic
- `.claude/rules/tdd.md` — TDD methodology
- `.claude/rules/code-quality.md` — 5 core review dimensions
- `.claude/rules/observability.md` — OTel, structured logging, metrics
- `.claude/rules/documentation.md` — Doc standards
- `.claude/rules/tech-debt.md` — Broken windows policy
- `.claude/rules/review-dimensions.md` — Full 20-dimension reference (on demand)
- `.claude/rules/recommended-tools.md` — MCP servers and tools

### Commands

| Command | Purpose |
|---------|---------|
| `/requirement <name>` | Gather and clarify a requirement |
| `/hld <name>` | Create high-level design |
| `/lld <name>` | Create low-level design with task checklist |
| `/implement <name>` | TDD implementation of LLD tasks |
| `/review <name>` | Code review (5 core dimensions) |
| `/staging <name>` | Deploy to staging, run integration tests |
| `/docs <name>` | Update all documentation |
| `/retro <name>` | Retrospective — capture learnings |
| `/status <name>` | Show pipeline progress for a feature |
| `/reset <name> [stage\|task] [options]` | Manually fix pipeline state |
| `/doctor` | Scan codebase for tech debt |
| `/onboard` | Regenerate this file with fresh analysis |
| `/preferences` | View or update preferences |
| `/tools` | Browse recommended MCP servers |
| `/explore <topic>` | Explore a new topic — code-first, curiosity-driven |
| `/spike <topic> <question>` | Time-boxed spike — hypothesis, minimal code, decision |
| `/crystallize <topic>` | Bridge exploration to building — synthesize into a requirement |
