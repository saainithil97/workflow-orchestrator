# Documentation Standards — Reference

Full documentation standards (doc types, sync rules, runbook structure, migration guide structure) are in `.dev-workflow/references/documentation.md`.

Load that file when you need detailed documentation guidance.

**When to load**: `/docs` stage only.
